#include<stdio.h>
int main()
{
    int arr[]={34,45,78,78,90,100};
    int low=0, high=5;
    int key,mid,r,i;
    printf("Enter a search value:");
    scanf("%d",&key);

    while(low<=high)
    {
        int mid=low+(high-low)/2;
        if(arr[mid])
    }
}